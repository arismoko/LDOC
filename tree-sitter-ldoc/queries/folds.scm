; Fold queries for LDOC

; Document and meta blocks
(meta_block) @fold

; Macro definitions and uses
(define_block) @fold

; Control flow blocks
(if_block) @fold
(repeat_block) @fold
(foreach_block) @fold

; Layout blocks
(table_block) @fold

; Comments
(block_comment) @fold
